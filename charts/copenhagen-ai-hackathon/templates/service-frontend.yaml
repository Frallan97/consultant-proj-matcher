apiVersion: v1
kind: Service
metadata:
  name: {{ include "copenhagen-ai-hackathon.fullname" . }}-frontend
  labels:
    {{- include "copenhagen-ai-hackathon.labels" . | nindent 4 }}
    component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copenhagen-ai-hackathon.selectorLabels" . | nindent 4 }}
    component: frontend
