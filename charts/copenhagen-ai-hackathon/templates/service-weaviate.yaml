{{- if .Values.weaviate.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "copenhagen-ai-hackathon.fullname" . }}-weaviate
  labels:
    {{- include "copenhagen-ai-hackathon.labels" . | nindent 4 }}
    component: weaviate
spec:
  type: {{ .Values.weaviate.service.type }}
  ports:
    - port: {{ .Values.weaviate.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copenhagen-ai-hackathon.selectorLabels" . | nindent 4 }}
    component: weaviate
{{- end }}
