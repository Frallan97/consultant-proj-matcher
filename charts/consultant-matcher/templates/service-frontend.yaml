apiVersion: v1
kind: Service
metadata:
  name: {{ include "consultant-matcher.fullname" . }}-frontend
  labels:
    {{- include "consultant-matcher.labels" . | nindent 4 }}
    component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "consultant-matcher.selectorLabels" . | nindent 4 }}
    component: frontend
